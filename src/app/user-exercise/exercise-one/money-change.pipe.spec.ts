import { MoneyChangePipe } from './money-change.pipe';

describe('MoneyChangePipe', () => {
  it('create an instance', () => {
    const pipe = new MoneyChangePipe();
    expect(pipe).toBeTruthy();
  });
});
